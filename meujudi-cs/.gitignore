node_modules/
dist/
out/
.next/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Environment
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# Cache de dados sensíveis (LGPD)
session.dat
cache/
*.pdf
*.tmp

# electron-builder — instaladores publicados ficam rastreados (ver
# .gitignore da raiz), só o resto do diretório de build (win-unpacked/,
# builder-debug.yml etc.) e releases antigas fora do padrão v* ficam de fora.
release/*
!release/MeuJudi-Sync-Setup-v*.exe
!release/MeuJudi-Sync-Setup-v*.exe.blockmap
dist-electron/
