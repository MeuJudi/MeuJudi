"use client";

import { useMemo, useState, useTransition } from "react";
import { closestCorners, DndContext, DragOverlay, PointerSensor, useDroppable, useSensor, useSensors, type DragEndEvent } from "@dnd-kit/core";
import { arrayMove, horizontalListSortingStrategy, SortableContext, useSortable, verticalListSortingStrategy } from "@dnd-kit/sortable";
import { CSS } from "@dnd-kit/utilities";
import { Check, CheckCircle2, CircleDotDashed, GripVertical, KanbanSquare, ListFilter, Plus, Search, Trash2, X } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { cn } from "@/lib/utils";
import { createTask, createTaskColumn, deleteTaskColumn, moveTask, renameTaskColumn, reorderTaskColumns } from "./actions";

export type TaskColumn = { id: string; name: string; position: number; color: string; is_default: boolean };
export type TaskItem = { id: string; title: string; description: string | null; priority: "alta" | "media" | "baixa"; due_date: string | null; kanban_column_id: string | null };
const priorityClass = { alta: "bg-[color-mix(in_srgb,var(--tenant-wine)_10%,transparent)] text-[var(--tenant-wine)]", media: "bg-[color-mix(in_srgb,var(--tenant-brass)_14%,transparent)] text-[#8c6425]", baixa: "bg-[color-mix(in_srgb,var(--tenant-moss)_10%,transparent)] text-[var(--tenant-moss)]" };
const priorityLabel = { alta: "Alta", media: "Média", baixa: "Baixa" };

function TaskCard({ task, handle }: { task: TaskItem; handle?: React.ReactNode }) {
  return <Card className="border-[var(--tenant-line)] bg-[var(--tenant-surface)] text-[var(--tenant-surface-foreground)] shadow-sm transition-shadow hover:shadow-md"><CardContent className="p-3"><div className="flex gap-2">{handle}<p className="min-w-0 flex-1 font-semibold text-[var(--tenant-surface-foreground)]">{task.title}</p></div>{task.description ? <p className="mt-2 text-sm leading-5 text-[var(--color-muted-foreground)]">{task.description}</p> : null}<Badge className={cn("mt-3 border-transparent uppercase tracking-wide", priorityClass[task.priority])}>{priorityLabel[task.priority]}</Badge></CardContent></Card>;
}

function SortableTask({ task }: { task: TaskItem }) {
  const { attributes, listeners, setNodeRef, transform, transition, isDragging } = useSortable({ id: task.id, data: { type: "task", columnId: task.kanban_column_id } });
  return <div ref={setNodeRef} style={{ transform: CSS.Transform.toString(transform), transition }} className={cn(isDragging && "opacity-40")} data-kanban-card><TaskCard task={task} handle={<button type="button" aria-label="Arrastar tarefa" className="cursor-grab text-[var(--color-muted-foreground)] active:cursor-grabbing" {...attributes} {...listeners}><GripVertical className="h-4 w-4" /></button>} /></div>;
}

function Column({ column, tasks, editing, draft, onEdit, onDraft, onSave, onCancel, onDelete }: { column: TaskColumn; tasks: TaskItem[]; editing: boolean; draft: string; onEdit: () => void; onDraft: (value: string) => void; onSave: () => void; onCancel: () => void; onDelete: () => void }) {
  const { setNodeRef: dropRef, isOver } = useDroppable({ id: column.id, data: { type: "column" } });
  const { attributes, listeners, setNodeRef, transform, transition, isDragging } = useSortable({ id: column.id, data: { type: "column" } });
  return <section ref={setNodeRef} style={{ transform: CSS.Transform.toString(transform), transition }} data-kanban-column className={cn("flex min-h-[460px] w-[320px] shrink-0 flex-col rounded-lg border border-[var(--tenant-line)] bg-[var(--tenant-surface-muted)] p-3 text-[var(--tenant-surface-foreground)]", isDragging && "opacity-50", isOver && "ring-2 ring-primary")}><header className="mb-3 space-y-2"><div className="flex items-center gap-2"><button type="button" aria-label="Arrastar coluna" className="cursor-grab text-[var(--color-muted-foreground)] active:cursor-grabbing" {...attributes} {...listeners}><GripVertical className="h-4 w-4" /></button><span className="h-3 w-3 rounded-full" style={{ backgroundColor: column.color }} />{editing ? <input autoFocus value={draft} onChange={(event) => onDraft(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter") onSave(); if (event.key === "Escape") onCancel(); }} className="min-w-0 flex-1 rounded border border-[var(--tenant-line)] bg-[var(--tenant-surface)] px-2 py-1 text-sm font-semibold text-[var(--tenant-surface-foreground)] outline-none" /> : <button type="button" onClick={onEdit} className="min-w-0 flex-1 truncate text-left text-sm font-semibold text-[var(--tenant-surface-foreground)]">{column.name}</button>}<span className="rounded-full bg-[var(--tenant-surface)] px-2 py-0.5 font-mono text-xs">{tasks.length}</span></div><div className="flex items-center justify-between"><div>{editing ? <><Button size="sm" className="mr-1 h-7 px-2" onClick={onSave}><Check className="h-3.5 w-3.5" /></Button><Button size="sm" variant="outline" className="h-7 px-2" onClick={onCancel}><X className="h-3.5 w-3.5" /></Button></> : <p className="text-xs text-[var(--color-muted-foreground)]">Arraste tarefas para esta coluna</p>}</div><Button size="sm" variant="ghost" className="h-7 px-2 text-[var(--tenant-wine)]" onClick={onDelete}><Trash2 className="h-3.5 w-3.5" /></Button></div></header><div ref={dropRef} className="min-h-0 flex-1 space-y-3"><SortableContext items={tasks.map((task) => task.id)} strategy={verticalListSortingStrategy}>{tasks.map((task) => <SortableTask key={task.id} task={task} />)}{tasks.length === 0 ? <div className="rounded-md border border-dashed border-[var(--tenant-line)] bg-[var(--tenant-surface)] p-4 text-center text-xs text-[var(--color-muted-foreground)]">Solte tarefas aqui.</div> : null}</SortableContext></div></section>;
}

export function TarefasView({ tenantId, columns, tasks }: { tenantId: string | null; columns: TaskColumn[]; tasks: TaskItem[] }) {
  const [view, setView] = useState<"kanban" | "lista">("kanban"), [localColumns, setColumns] = useState(columns), [localTasks, setTasks] = useState(tasks), [query, setQuery] = useState(""), [editingId, setEditingId] = useState<string | null>(null), [draft, setDraft] = useState(""), [active, setActive] = useState<TaskItem | null>(null), [deleting, setDeleting] = useState<TaskColumn | null>(null), [creating, setCreating] = useState(false), [title, setTitle] = useState(""), [description, setDescription] = useState(""), [priority, setPriority] = useState<TaskItem["priority"]>("media");
  const [pending, startTransition] = useTransition(); const sensors = useSensors(useSensor(PointerSensor, { activationConstraint: { distance: 6 } }));
  const filtered = useMemo(() => { const term = query.trim().toLowerCase(); return term ? localTasks.filter((task) => `${task.title} ${task.description ?? ""} ${priorityLabel[task.priority]}`.toLowerCase().includes(term)) : localTasks; }, [query, localTasks]);
  const grouped = useMemo(() => localColumns.map((column) => ({ ...column, tasks: filtered.filter((task) => task.kanban_column_id === column.id) })), [localColumns, filtered]);
  function dragEnd(event: DragEndEvent) { setActive(null); const { active, over } = event; if (!over || active.id === over.id) return; const type = active.data.current?.type, overType = over.data.current?.type; if (type === "column" && overType === "column") { const oldIndex = localColumns.findIndex((x) => x.id === active.id), newIndex = localColumns.findIndex((x) => x.id === over.id); const ordered = arrayMove(localColumns, oldIndex, newIndex).map((x, position) => ({ ...x, position })); setColumns(ordered); startTransition(() => reorderTaskColumns(ordered.map((x) => x.id))); } if (type === "task") { const target = overType === "task" ? String(over.data.current?.columnId) : String(over.id); const task = localTasks.find((x) => x.id === active.id); if (!task || task.kanban_column_id === target || !localColumns.some((x) => x.id === target)) return; setTasks((current) => current.map((x) => x.id === task.id ? { ...x, kanban_column_id: target } : x)); startTransition(() => moveTask(task.id, target)); } }
  function addTask() { if (!tenantId || !localColumns[0] || !title.trim()) return; const columnId = localColumns[0].id; startTransition(async () => { const task = await createTask(tenantId, columnId, title, description, priority); setTasks((current) => [task as TaskItem, ...current]); setCreating(false); setTitle(""); setDescription(""); }); }
  return <div className="w-full space-y-6"><header className="flex flex-wrap items-start justify-between gap-4"><div><h1 className="font-display text-3xl font-bold text-[var(--color-card-foreground)]">Tarefas</h1><p className="mt-2 max-w-3xl text-sm text-[var(--color-muted-foreground)]">Organize o trabalho interno do escritório em um quadro próprio, ligado ou não a processos.</p></div><Badge className="rounded-full bg-[color-mix(in_srgb,var(--tenant-moss)_14%,transparent)] text-[var(--tenant-moss)]">{localTasks.length} tarefa{localTasks.length === 1 ? "" : "s"}</Badge></header><section className="grid gap-3 md:grid-cols-3">{[{ label: "Total", value: localTasks.length, icon: ListFilter }, { label: "Em andamento", value: grouped.find((x) => /andamento/i.test(x.name))?.tasks.length ?? 0, icon: CircleDotDashed }, { label: "Concluídas", value: grouped.find((x) => /conclu/i.test(x.name))?.tasks.length ?? 0, icon: CheckCircle2 }].map((metric) => <Card key={metric.label} className="border-[var(--tenant-line)] bg-[var(--tenant-surface)]"><CardContent className="flex items-center justify-between p-4"><div><p className="text-sm text-[var(--color-muted-foreground)]">{metric.label}</p><p className="mt-1 text-3xl font-semibold">{metric.value}</p></div><metric.icon className="h-6 w-6 text-primary" /></CardContent></Card>)}</section><Card className="border-[var(--tenant-line)] bg-[var(--tenant-surface)]"><CardContent className="space-y-4 p-4"><div className="flex flex-wrap items-center justify-between gap-3"><div className="inline-flex rounded-lg border border-[var(--tenant-line)] bg-[var(--tenant-surface-muted)] p-1">{[["kanban", KanbanSquare, "Kanban"], ["lista", ListFilter, "Lista"]].map(([value, Icon, label]) => <button key={value as string} type="button" onClick={() => setView(value as "kanban" | "lista")} className={cn("inline-flex items-center gap-2 rounded-md px-3 py-2 text-sm font-medium", view === value ? "bg-[var(--tenant-surface)] text-[var(--tenant-brass)] shadow-sm" : "text-[var(--color-muted-foreground)]")}><Icon className="h-4 w-4" />{label as string}</button>)}</div><div className="flex min-w-[260px] flex-1 justify-end gap-2"><Button type="button" onClick={() => setCreating(true)} disabled={!tenantId}><Plus className="h-4 w-4" />Tarefa</Button>{view === "kanban" ? <Button type="button" variant="outline" onClick={() => startTransition(async () => { if (!tenantId) return; const c = await createTaskColumn(tenantId); setColumns((x) => [...x, c as TaskColumn]); })} disabled={!tenantId || pending}><Plus className="h-4 w-4" />Coluna</Button> : null}<label className="flex min-w-[230px] items-center gap-2 rounded-md border border-[var(--tenant-line)] px-3 py-2 text-sm"><Search className="h-4 w-4 text-[var(--color-muted-foreground)]" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Buscar tarefas" className="w-full bg-transparent outline-none" /></label></div></div>{view === "lista" ? <div className="space-y-3">{filtered.map((task) => <TaskCard key={task.id} task={task} />)}{filtered.length === 0 ? <p className="rounded-md border border-dashed border-[var(--tenant-line)] p-5 text-sm text-[var(--color-muted-foreground)]">Nenhuma tarefa encontrada.</p> : null}</div> : <DndContext sensors={sensors} collisionDetection={closestCorners} onDragStart={(event) => setActive(localTasks.find((task) => task.id === event.active.id) ?? null)} onDragEnd={dragEnd}><div className="overflow-x-auto pb-2"><SortableContext items={localColumns.map((x) => x.id)} strategy={horizontalListSortingStrategy}><div className="flex min-h-[500px] gap-4">{grouped.map((column) => <Column key={column.id} column={column} tasks={column.tasks} editing={editingId === column.id} draft={draft} onEdit={() => { setEditingId(column.id); setDraft(column.name); }} onDraft={setDraft} onSave={() => { const name = draft.trim(); if (!name) return; setColumns((current) => current.map((x) => x.id === column.id ? { ...x, name } : x)); setEditingId(null); startTransition(() => renameTaskColumn(column.id, name)); }} onCancel={() => setEditingId(null)} onDelete={() => setDeleting(column)} />)}</div></SortableContext></div><DragOverlay>{active ? <div className="w-[300px] rotate-1 shadow-xl"><TaskCard task={active} /></div> : null}</DragOverlay></DndContext>}</CardContent></Card>{creating ? <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/45 p-4"><div className="w-full max-w-lg rounded-lg border border-[var(--tenant-line)] bg-[var(--tenant-surface)] p-5"><h2 className="font-display text-2xl font-bold">Nova tarefa</h2><input autoFocus value={title} onChange={(event) => setTitle(event.target.value)} placeholder="Título da tarefa" className="mt-4 w-full rounded-md border border-[var(--tenant-line)] bg-transparent px-3 py-2 outline-none" /><textarea value={description} onChange={(event) => setDescription(event.target.value)} placeholder="Descrição (opcional)" className="mt-3 min-h-24 w-full rounded-md border border-[var(--tenant-line)] bg-transparent px-3 py-2 outline-none" /><select value={priority} onChange={(event) => setPriority(event.target.value as TaskItem["priority"])} className="mt-3 rounded-md border border-[var(--tenant-line)] bg-transparent px-3 py-2"><option value="alta">Alta</option><option value="media">Média</option><option value="baixa">Baixa</option></select><div className="mt-5 flex justify-end gap-2"><Button variant="outline" onClick={() => setCreating(false)}>Cancelar</Button><Button onClick={addTask} disabled={!title.trim() || pending}>Criar tarefa</Button></div></div></div> : null}{deleting ? <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/45 p-4"><div className="w-full max-w-lg rounded-lg border border-[var(--tenant-line)] bg-[var(--tenant-surface)] p-5"><h2 className="font-display text-2xl font-bold">Excluir coluna</h2><p className="mt-2 text-sm text-[var(--color-muted-foreground)]">As tarefas serão transferidas para outra coluna.</p><div className="mt-4 flex flex-wrap gap-2">{localColumns.filter((x) => x.id !== deleting.id).map((x) => <Button key={x.id} variant="outline" onClick={() => { setTasks((tasks) => tasks.map((task) => task.kanban_column_id === deleting.id ? { ...task, kanban_column_id: x.id } : task)); setColumns((columns) => columns.filter((column) => column.id !== deleting.id)); setDeleting(null); startTransition(() => deleteTaskColumn(deleting.id, x.id)); }}>{x.name}</Button>)}</div><Button className="mt-5" variant="ghost" onClick={() => setDeleting(null)}>Cancelar</Button></div></div> : null}</div>;
}
